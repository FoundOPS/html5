#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../grunt-recess/bin/grunt-recess" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../grunt-recess/bin/grunt-recess" "$@"
  ret=$?
fi
exit $ret
