#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../grunt-less/bin/grunt-less" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../grunt-less/bin/grunt-less" "$@"
  ret=$?
fi
exit $ret
